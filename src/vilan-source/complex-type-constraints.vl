
mod tc {
	fun is_natural(n: i32): Result<void, str> {
		n.is_whole() else ret Err("value must be whole");
		n >= 1 else ret Err("value must be greater than or equal to 1");
		Ok(void)
	}
}

fun f(x: i32 + tc::is_natural) {}

fun f(x: i32 + |x| x < 10) {}
