[package]
name = "vilan-core"
version = "0.10.0"
edition = "2024"
license = "MIT OR Apache-2.0"

[dependencies]
chumsky = "0.12.0"
indexmap = "2.14.0"
serde = { version = "1", features = ["derive"] }
toml = "0.8"
