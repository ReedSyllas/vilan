import std;

trait Default {
	fun default(): Self;
}

struct Id {
	n: u32,
}

impl Id {
	fun new(n: u32) {
		Id { n }
	}
}

impl Id with Default {
	fun default() {
		Id::new(0)
	}
}

fun main() {
	let my_id = Id::default();
	
	std::print(my_id);
}
