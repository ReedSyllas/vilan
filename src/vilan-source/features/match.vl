
// Match block
match 100 {
	0 => { print("0"); }
	0..50 => { print("0 to 50"); }
	50.. => { print("above 50"); }
	_ => { print("something else"); }
}

// Match expression
let value = match Some(100) {
	Some(x = 0..50) => 50,
	Some(x) => x,
	None => 0,
};
